# Optional native iOS package set.
#
# These packages are not installed by generate_project.sh by default because
# they need iOS-specific wheels per device/simulator architecture. Use
# scripts/install_python_native_packages.sh to populate PythonApp/native.
#
# Current status:
# - numpy has CPython 3.14 iOS wheels on BeeWare's secondary wheel index.
# - Pillow has CPython 3.14 iOS wheels on PyPI.
# - pdf2image and reportlab resolve for iOS when Pillow's native wheel is
#   installed per architecture.
# - upstream lxml, pandas, and scipy do not currently resolve for CPython 3.14
#   iPhoneOS via PyPI + BeeWare's secondary wheel index. A pure-Python
#   lxml.etree compatibility package is staged in the app package root for
#   tested cached Documents helper behavior.
# - upstream python-docx does not currently resolve for CPython 3.14 iPhoneOS
#   because its upstream lxml dependency is unavailable for this target. A
#   pure-Python python-docx compatibility package is staged in the app package
#   root for tested cached Documents helper behavior.
numpy==2.3.5.post1
pillow==12.2.0
pdf2image==1.17.0
reportlab==4.5.0

# Keep probing these; do not enable until compatible iOS wheels exist.
# lxml
# python-docx
# pandas
# scipy
